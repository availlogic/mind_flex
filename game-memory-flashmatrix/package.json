{
  "name": "game-memory-flashmatrix",
  "version": "0.1.0",
  "private": true,
  "description": "MindFlex benchmark sub-game: spatial memory grid (Flash Matrix).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "python3 -m http.server 5174"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
